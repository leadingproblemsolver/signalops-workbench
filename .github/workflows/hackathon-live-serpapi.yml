name: hackathon-live-serpapi-receipt

on:
  workflow_dispatch:
    inputs:
      query:
        description: Live market query
        required: false
        default: AI agent production reliability hiring OR looking for help
      limit:
        description: Number of organic results to accept
        required: false
        default: '3'

jobs:
  live-receipt:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Prepare live receipt directory
        run: mkdir -p artifacts/live

      - name: Require live credentials
        env:
          SERPAPI_API_KEY: ${{ secrets.SERPAPI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        shell: bash
        run: |
          set -euo pipefail
          test -n "$SERPAPI_API_KEY" || (echo 'FAIL: SERPAPI_API_KEY repository secret is absent from this runner' && exit 1)
          test -n "$OPENAI_API_KEY" || (echo 'FAIL: OPENAI_API_KEY repository secret is absent from this runner' && exit 1)
          echo "PASS: SERPAPI_API_KEY reached runner (masked; length=${#SERPAPI_API_KEY})"
          echo "PASS: OPENAI_API_KEY reached runner (masked; length=${#OPENAI_API_KEY})"

      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Verify SerpApi credential acceptance
        env:
          SERPAPI_API_KEY: ${{ secrets.SERPAPI_API_KEY }}
        run: |
          python - <<'PY'
          import json
          import os
          from urllib.parse import urlencode
          from urllib.request import urlopen

          key = os.environ.get('SERPAPI_API_KEY', '').strip()
          if not key:
              raise SystemExit('FAIL: SERPAPI_API_KEY is empty after runner injection')
          params = urlencode({
              'engine': 'google',
              'q': 'SignalOps credential acceptance check',
              'api_key': key,
              'output': 'json',
              'num': 1,
          })
          with urlopen(f'https://serpapi.com/search.json?{params}', timeout=20) as response:
              payload = json.loads(response.read().decode('utf-8'))
          if payload.get('error'):
              raise SystemExit(f"FAIL: SerpApi rejected the injected credential: {payload['error']}")
          metadata = payload.get('search_metadata') or {}
          print(f"PASS: SerpApi accepted injected credential; search_id_present={bool(metadata.get('id'))}")
          PY

      - name: Install SignalOps
        run: python -m pip install -e .

      - name: Run credentialed hackathon acceptance path
        env:
          SERPAPI_API_KEY: ${{ secrets.SERPAPI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: gpt-5.6-luna
          SIGNALOPS_DB: artifacts/live/hackathon-live.db
        run: |
          python scripts/hackathon_live_smoke.py \
            --query '${{ inputs.query }}' \
            --limit '${{ inputs.limit }}' \
            --output artifacts/live/serpapi_hackathon_receipt.json

      - name: Upload sanitized live receipt
        uses: actions/upload-artifact@v4
        with:
          name: serpapi-live-${{ github.sha }}
          path: artifacts/live/serpapi_hackathon_receipt.json
          if-no-files-found: error
