# Architecture

Channel policy → evidence surface → deterministic score → permission gate → transactional SQLite projection + decision event → ranked action/handoff/CRM.

A modular local monolith is sufficient until multi-user or connector evidence exists.
